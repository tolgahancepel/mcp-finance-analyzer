[project]
name = "mcp-finance-analyzer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "tolgahancepel", email = "tolgahan.cepel@gmail.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
mcp-finance-analyzer = "mcp_finance_analyzer:main"

[build-system]
requires = ["uv_build>=0.11.8,<0.12.0"]
build-backend = "uv_build"
